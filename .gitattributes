# Shell scripts must use LF so they run inside Linux containers, even when the
# repo is checked out on Windows (where core.autocrlf may rewrite them to CRLF).
# A CRLF in a script's shebang makes the kernel look for "/bin/sh\r" and the
# container fails with: exec ...: no such file or directory.
*.sh text eol=lf
